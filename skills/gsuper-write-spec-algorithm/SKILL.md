---
name: gsuper-write-spec-algorithm
description: >
  Write specs/algorithm/ using algorithm-shape (I/O, happy, fallback, keep/drop).
  Recipe only. Not HTTP. Not import law.
---

# Write spec — algorithm

Fill [../gsuper-write-spec/references/algorithm-shape.md](../gsuper-write-spec/references/algorithm-shape.md).

**Job:** X in → steps → Y out.

## Apply

- [ ] ## Transforms, ## I/O, ## Happy, ## Fallback, ## Keep / drop, ## Limits
- [ ] Happy vs fallback named separately
- [ ] Each happy step what + why
- [ ] No CC-01 shalls; no layer import table as the body

Next: **suggest** feature or plan — **wait**. Do **not** auto-write them.
