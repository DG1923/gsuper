---
name: gsuper-workflow-learn-material
description: Lesson + runnable sample + quiz from verified implementation (not the pack)
---

Run the **gsuper-learn-material** skill. Use only after implement has evidence (or the user names a shipped unit). List 3–6 concepts from live code; wait for the user to pick one; then write `gsuper-material-<repo>-<ticket>-<concept>.md` plus a stdlib sample. Validate theory == sample == production (run the sample). Does not block review. Not AC. Not `gsuper-learn-pack`.
